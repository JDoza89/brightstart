---
description: Bulk-cache the Figma file via the figma-snapshot script.
---

Run the figma-snapshot script (`scripts/figma-snapshot.mjs`) to cache the Figma file's metadata, variables, and screenshots locally so subsequent steps don't burn tokens on repeated MCP calls.
