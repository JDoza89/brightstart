---
description: Build React components for each block matching the Figma design.
---

Build the React components for each Storyblok block in accordance to the corresponding figma design in projects/brightstart.

First, read the project to confirm framework version, routing, styling system, and JS vs TS. Match the project's existing conventions, not the design context output (which assumes Tailwind).

Register all blocks, wire the dynamic route to fetch page + header + footer in parallel, and configure next/image for the asset CDN. Match the Figma styling.
